server:
  port: 8086
spring:
  application:
    name: azdm-account
  cloud:
    config:
      label:
      profile: pro
      discovery:
        enabled: true
        service-id: azdm-config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8889/eureka/


